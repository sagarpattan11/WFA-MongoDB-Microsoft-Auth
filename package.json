{
  "name": "workforce-analytics-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Enterprise Workforce Analytics Platform (WFA)",
  "scripts": {
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"blue,green\" \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:frontend": "npm run --prefix frontend dev",
    "dev:backend": "npm run --prefix backend dev",
    "lint": "npm run --prefix backend lint && npm run --prefix frontend lint",
    "typecheck": "npm run --prefix backend typecheck && npm run --prefix frontend typecheck",
    "test": "npm run --prefix backend test && npm run --prefix frontend test",
    "test:e2e": "npm run --prefix frontend test:e2e",
    "build": "npm run --prefix backend build && npm run --prefix frontend build",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3"
  },
  "author": "Sagar Pattan",
  "license": "MIT"
}
